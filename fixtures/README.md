# OUI fixtures

Fixtures are compiler-independent inputs and expected outputs used to develop and verify OUI.

## Fixture sets

- [`reference`](reference/README.md) is the domain-neutral vertical-slice application.

Fixture data is not a production ontology, customer configuration, or source specification bundle. It represents the application IR boundary consumed by OUI.

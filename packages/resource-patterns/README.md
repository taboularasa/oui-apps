# `@oui/resource-patterns`

`@oui/resource-patterns` compiles collection and detail interactions into
domain-neutral semantic models and provides accessible default renderers.

Table, list, and virtualized collection presentations consume the same compiled
contract. Presentation can therefore change without changing the application
IR's filtering, sorting, pagination, selection, navigation, or field meaning.

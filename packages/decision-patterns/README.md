# `@oui/decision-patterns`

`@oui/decision-patterns` compiles evidence-backed decision declarations into a
domain-neutral policy model and realizes their lifecycle with XState.

The package keeps evidence review, authority, preconditions, alternative-specific
reasons, confirmation, consequential command execution, conflict recovery, and
outcome provenance behind OUI-owned contracts.
